<Window x:Class="Lab4.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab4"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="400">
    <StackPanel HorizontalAlignment="Center" Margin="10">
        <Button x:Name="readBut" Margin="5" Width="150" Height="30" Content="Read From File" Click="onReadButton"/>
        <Label x:Name="readTimeLabel" Content="Read Time: 0"/>
        <TextBox x:Name="searchWord" Margin="5" Height="25"></TextBox>
        <Button x:Name="searchBut" Margin="5" Height="30" Content="Search" Click="onSearchButton"/>
        <Label x:Name="searchTimeLabel" Content="Search time: 0" />
        <Label Content="Search result:" />
        <ListBox x:Name="resultListBox" ScrollViewer.VerticalScrollBarVisibility="Visible" Height="250">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Word}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </StackPanel>
</Window>

